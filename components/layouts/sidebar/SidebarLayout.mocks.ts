import { ISidebarLayout } from './SidebarLayout';

const base: ISidebarLayout = { sampleTextProp: 'sample Text Prop' };

// const alt: ISidebarLayout = { sampleTextProp: 'Sample Text Prop Alt version' };

export const mockSidebarLayoutProps = {
  base,
  // alt
};
