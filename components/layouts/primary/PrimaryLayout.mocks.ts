import { IPrimaryLayout } from './PrimaryLayout';

const base: IPrimaryLayout = { sampleTextProp: 'sample Text Prop' };

// const alt: IPrimaryLayout = { sampleTextProp: 'Sample Text Prop Alt version' };

export const mockPrimaryLayoutProps = {
  base,
  // alt
};
