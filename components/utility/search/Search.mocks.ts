import { ISearch } from './Search';

const base: ISearch = { sampleTextProp: 'sample Text Prop' };

// const alt: ISearch = { sampleTextProp: 'Sample Text Prop Alt version' };

export const mockSearchProps = {
  base,
  // alt
};
