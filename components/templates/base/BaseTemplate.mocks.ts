import { IBaseTemplate } from './BaseTemplate';

const base: IBaseTemplate = { sampleTextProp: 'sample Text Prop' };

// const alt: IBaseTemplate = { sampleTextProp: 'Sample Text Prop Alt version' };

export const mockBaseTemplateProps = {
  base,
  // alt
};
